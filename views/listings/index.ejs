<% layout("layouts/boilerplate.ejs") %>
    <body>
        <h3>All listings</h3>
        <!-- <form method="get" action="/listings/new">
            <button>Create New Listing</button>
        </form> -->
        <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">

            <% for (let item of listings) { %>

                <a href="/listings/<%=item._id%>" class="listing-link">

                    <div class="card col">
                        <div class="card-img-overlay"></div>
                        <img src="<%=item.image.url%>" class="card-img-top" alt="listing image" style="height: 20rem;">
                        
                        <div class="card-body">
                            <p class="card-text">
                                <b><%= item.title%> </b> <br>
                                &#8377;<%=item.price.toLocaleString("en-IN")%> per night
                            </p>
                        </div>
                    </div>

                </a>

            <% } %>

        </div>
        


        <!-- <ul>
            <% for (let item of listings) { %>

                <li><a href="/listings/<%= item._id %>"><%= item.title %></a></li>

            <% } %>

        </ul> -->
    </body>
